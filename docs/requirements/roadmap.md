# 制約・前提条件 / 今後の拡張候補

[← 要件定義書に戻る](../requirements.md)

## 制約・前提条件

**データはブラウザ単位。** 保存先は利用中のブラウザ・端末の `localStorage` のみ。別の端末やブラウザ、シークレットウィンドウでは別データになり、ブラウザの閲覧データを削除すると内容も消える。バックアップやマルチデバイス同期は現時点で提供しない。

## 今後の拡張候補

優先度の目安つきで、想定される拡張を列挙する。実装するかどうかは都度判断する。

| 優先度 | 候補機能 | 状態 |
| --- | --- | --- |
| 高 | カードの説明欄・ラベル（色分け） | 未着手 |
| 高 | 完了したタスクのアーカイブ表示 | 未着手 |
| 中 | 複数ボードの作成・切り替え | 未着手 |
| 中 | カードの検索・絞り込み | `backend`/`frontend` 版で実装済み（キーワード・優先度・リストIDによる絞り込み） |
| 低 | バックエンド（サーバー・DB）導入によるマルチデバイス同期 | 一部実装済み。`backend`/`frontend` でリスト表示・カードの検索/絞り込み/並び替え/作成/編集/削除/ドラッグ&ドロップ移動に対応済み。ただし複数端末間での同期を利用するにはこのバックエンド版のデプロイ・運用が別途必要（現状はローカル開発環境のみ） |
| 低 | ユーザー認証、複数人での共有・共同編集 | 未着手 |

バックエンド導入やマルチボード対応時のデータモデルは [データモデル](data-model.md) のER図、採用予定の技術スタックは [技術スタック](tech-stack.md) を参照。実装状況の詳細は [backend/README.md](../../backend/README.md)・[frontend/README.md](../../frontend/README.md) を参照。

---

[← 要件定義書に戻る](../requirements.md)
