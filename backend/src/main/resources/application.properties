spring.application.name=backend

# --- H2 (デフォルト: 起動確認用インメモリDB) ---
spring.datasource.url=jdbc:h2:mem:taskboard
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# --- PostgreSQL (本番移行時にこちらへ切り替える) ---
# H2の設定をコメントアウトし、以下のコメントを解除して使用する。
# build.gradle.kts で org.postgresql:postgresql の依存関係も有効化すること。
#
# spring.datasource.url=jdbc:postgresql://localhost:5432/taskboard
# spring.datasource.driver-class-name=org.postgresql.Driver
# spring.datasource.username=taskboard
# spring.datasource.password=change-me
# spring.jpa.hibernate.ddl-auto=validate
# spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
